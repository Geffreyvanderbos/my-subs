services:
  my-subs:
    build: .
    container_name: my-subs
    restart: unless-stopped
    ports:
      - "8080:3000"
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - my-subs-network

networks:
  my-subs-network:
    driver: bridge
